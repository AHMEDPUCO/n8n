stages:
  - security-scan

devsecops-placeholder:
  stage: security-scan
  image: alpine:latest
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push"'
  script:
    - echo "Pipeline OK. n8n (GitLab Trigger) maneja los escaneos por fuera del CI."
